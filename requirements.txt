# training. Kaggle already has tensorflow, torch (CUDA), opencv + matplotlib (used by keras.visualization);
# elsewhere also install: tensorflow torch torchvision opencv-python-headless matplotlib
keras-hub
pycocotools
transformers   # Grounding DINO for auto-labeling only
dvc            # S3 plugin is installed by the notebook only when AWS secrets exist (it downgrades botocore)
mlflow
pyopenssl>=26.4  # Kaggle ships pyopenssl 24 (needs cryptography<44); dvc/mlflow upgrade cryptography, which breaks `import tensorflow`
